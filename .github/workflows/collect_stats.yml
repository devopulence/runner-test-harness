name: Collect Monitoring Stats

on:
  schedule:
    # Runs every 6 hours: midnight, 6am, noon, 6pm UTC
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:
    inputs:
      org:
        description: 'GitHub org to collect from'
        required: false
        default: 'devopulence'
      max_pages:
        description: 'Max API pages per repo for workflow runs'
        required: false
        default: '10'
      max_runs:
        description: 'Max runs to fetch jobs for per repo'
        required: false
        default: '50'
      skip_jobs:
        description: 'Skip job collection (faster, runs only)'
        required: false
        type: boolean
        default: false

env:
  GITHUB_TOKEN: ${{ secrets.GH_MONITORING_TOKEN }}
  DEFAULT_ORG: 'devopulence'

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install requests python-dotenv

      - name: Run scheduled collection
        run: |
          ORG="${{ github.event.inputs.org || env.DEFAULT_ORG }}"
          MAX_PAGES="${{ github.event.inputs.max_pages || '10' }}"
          MAX_RUNS="${{ github.event.inputs.max_runs || '50' }}"
          SKIP_JOBS="${{ github.event.inputs.skip_jobs || 'false' }}"

          ARGS="--org ${ORG} --max-pages ${MAX_PAGES}"

          if [ "${MAX_RUNS}" != "" ]; then
            ARGS="${ARGS} --max-runs ${MAX_RUNS}"
          fi

          if [ "${SKIP_JOBS}" = "true" ]; then
            ARGS="${ARGS} --skip-jobs"
          fi

          echo "Running: python -m src.monitoring.scheduled_collect ${ARGS} -v"
          python -m src.monitoring.scheduled_collect ${ARGS} --token "${{ secrets.GH_MONITORING_TOKEN }}" -v

      - name: Commit monitoring data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add monitoring_data/
          if git diff --staged --quiet; then
            echo "No new monitoring data to commit"
          else
            DATE=$(date -u +%Y-%m-%d)
            git commit -m "chore: collect monitoring stats for ${DATE}"
            git pull --rebase
            git push
          fi

      - name: Upload monitoring data as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-data-${{ github.run_id }}
          path: monitoring_data/
          retention-days: 30